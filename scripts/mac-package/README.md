# Файлы для Mac-пакета

Раньше лежали в `public/` и поэтому попадали и в сборку сайта, и на публичный
адрес. Для сайта они не нужны — это помощники для локального показа на Mac.

При сборке пакета для Mac копировать их рядом с папкой `site`.
